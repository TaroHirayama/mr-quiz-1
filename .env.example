# MR/PR Quiz Bot - 環境変数テンプレート
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env

# Google Cloud Project ID (also used for Vertex AI Gemini authentication)
GOOGLE_CLOUD_PROJECT=your-project-id

# APIサーバーポート（デフォルト: 3000）
PORT=3000

# Firestore Emulator Host（ローカル開発用）
# npm run emulator で起動後、コメントを外す
FIRESTORE_EMULATOR_HOST=localhost:8080

# Node環境（development / production）
NODE_ENV=development

# Webhook Secret（GitHub/GitLab署名検証用）
# 本番環境では必須。ローカル開発では未設定で検証スキップ
# GitHub: Settings > Webhooks > Secret で設定した値
# GitLab: Settings > Webhooks > Secret token で設定した値
# WEBHOOK_SECRET=your-webhook-secret

# GitHub Personal Access Token（PR差分取得、コメント投稿用）
# https://github.com/settings/tokens/new から取得
# 必要なスコープ: repo (Full control of private repositories)
# GITHUB_TOKEN=ghp_your_github_personal_access_token

# GitLab Personal Access Token（MR差分取得、コメント投稿用）
# https://gitlab.com/-/profile/personal_access_tokens から取得
# 必要なスコープ: api, read_api, write_repository
# GITLAB_TOKEN=glpat-your_gitlab_personal_access_token

# Bot動作のベースURL（PRコメント内のリンク用）
# ローカル開発: http://localhost:3000
# 本番環境: https://your-app.run.app
APP_BASE_URL=http://localhost:3000
